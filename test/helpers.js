const {Op, Model, DataTypes, Sequelize} = require('sequelize');
const { User } = require('../api/model/models');

module.exports = {
  createUser: async () => {
    
    // const token = user.generateAuthToken(user);
    
  }
}